const page = () => {
  function guesser(num) {
    num = document.getElementById();
  }

  return (
    <div>
      <div>
        <h1>Guessing Game</h1>
        <h2>Can you guess which number I'm thinking of between 0 and 100?</h2>
        <button>Start</button>
      </div>
    </div>
  );
};

export default page;
